import { combineReducers } from 'redux';

// just leaving this in for now to avoid getting warning but can
// take away later once you create a real reducer
import StartingReducer from './starting_reducer';

export default combineReducers({
  StartingReducer
});
